import { JWTToken, JWTBody, JWTHeader, HeaderOptions, EncodingKey } from '../types/jwt';
declare class Encoder {
    body: JWTBody;
    header: JWTHeader;
    constructor(body: JWTBody, key: EncodingKey, headerOptions?: HeaderOptions);
    buildHeader(): JWTHeader;
    encodeAndSign(): JWTToken;
}
export default Encoder;
